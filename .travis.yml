language: node_js
node_js: 8

sudo: required
services:
  - docker

script:
  - yarn test
  - docker-compose build

after_success:
  - echo $DOCKER_PASS | docker login --username $DOCKER_USER --password-stdin docker.ciffelia.com
  - docker-compose push
